<view class="page">

  <view class="page__hd">
  </view>

  <view class="section">
    <view class="section__title">{{lang_picker}}</view>
    <picker bindchange="languageBindPickerChange" value="{{index}}" range="{{array}}">
      <view class="picker">
        {{lang_picker_placeholder}} {{array[index]}}
      </view>
    </picker>
  </view>

</view>